<div class='well'>
  <%= form_for [:admin, @forum_catalog], :html => {:class => 'form-horizontal', :id => 'edit-forum-catalog'} do |f| %>
    <fieldset>
      <legend><%= @forum_catalog.new_record? ? '添加分类' : '修改分类'%></legend>
      <div class='form-group'>
        <%= f.label :name, '*名称', :class => 'col-lg-2 control-label' %>
        <div class='col-lg-10'>
          <%= f.text_field :name, :class => 'form-control' %>
        </div>
      </div>
      <div class='form-group'>
        <div class='col-lg-10 col-lg-offset-2'>
          <%= f.submit '保存', :class => 'btn btn-primary' %>
        </div>
      </div>
    </fieldset>
  <% end %>
</div>
