<li>
  <div class='name'><%= comment.commenter.name %></div>
  <div class='content'><%= comment.content %></div>
  <div class='addition'>
    <span><%= l comment.created_at %></span>
    <%= link_to '删除', [@question, comment], :method => :delete, :remote => true, :class => :delete, :confirm => '确实要删除这个评论吗？' if can?(:destroy, comment) %>
  </div>
</li>
