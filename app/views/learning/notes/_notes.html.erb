<div class="media-items">
	<%= render :collection => notes, :partial => 'learning/notes/note' %>
</div>

<%= paginate notes, :remote => true, :params => {:controller => 'learning/notes', :action => :index, :video_watching_id => video_watching.id} %>